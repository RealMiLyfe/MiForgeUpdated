/**
 * MiForge Orchestration — Layer 3
 *
 * Exports:
 *   SwarmOrchestrator — parallel free models + consensus
 *   (Future: LangGraph, CrewAI, Mastra integrations)
 */

export { SwarmOrchestrator, swarm } from './swarm.js';
export type { SwarmConfig, SwarmResult, SpecialistRole } from './swarm.js';
